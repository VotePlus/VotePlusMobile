import * as React from 'react'
import { Text, SafeAreaView } from 'react-native'

const ListComponent = () => {
    return(
        <SafeAreaView>
            <Text>
                List component
            </Text>
        </SafeAreaView>
    )
}

export default ListComponent;